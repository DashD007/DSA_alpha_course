public class lecture_168 {
    public static void main(String[] args) {
        // in this lecture we will learn about polymorphism;
        // poly means many and morphism means forms..
        // when a same object tries to do multiple different works then it is called polymorphism
        // for example --> we created a constructor ..which have different works based on parameters
        // there are two types of polymorphism --> compile time polymorphism(method overloading) , runtime polymorphism(method overriding).
    }
}
